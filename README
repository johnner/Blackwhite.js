Blackwhite is a library that shows your web page in grayscale view.
It's parses CSS rules and replace colors with grayscale equivalent.

Script also convert images on page using canvas.

Tested in FF > 4.0 and Chrome
Works not so cool in other browsers yet

